#!/usr/bin/env python3
#MISE description="sync prod -> dev: dump prod to a file, then restore that file into dev"
#MISE depends=["sql-client-check"]
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent / 'lib'))
from sql_task_engine import cli_sync  # noqa: E402

sys.exit(cli_sync('prod', 'dev'))
