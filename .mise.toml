[env]
# Activate the project venv so mise tasks can use Python and project CLI tools
_.python.venv = "python/rpkilog/.venv"

[tools]
# Atlas Community Edition: applies the snapshot-db schema migrations
"atlas-community" = "1.2.0"
